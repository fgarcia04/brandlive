{% extends 'base.html.twig' %}
{% use 'ClientesBundle:Cliente:form.html.twig' %}

{% block body %}
    <h3>Nuevo Cliente</h3>
    {% include 'ClientesBundle:Cliente:messages/succes.html.twig' %}
    {% block form %}
        {{  parent()  }}
        <button type="submit" class="btn btn-primary">Agregar</button>
        <a href="{{ path('home') }}" type="button" class="btn btn-secondary">Cancelar</a>
        {{ form_end(form) }}
    {% endblock %}
{% endblock %}